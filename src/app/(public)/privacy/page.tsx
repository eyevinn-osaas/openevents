import { InfoPageShell } from '@/components/layout/InfoPageShell'

export default function PrivacyPage() {
  return (
    <InfoPageShell
      title="Privacy Policy"
      description="This temporary privacy page is in place so users can access policy information while the final legal text is being prepared."
    />
  )
}
